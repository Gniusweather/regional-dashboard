--- a/index.html
+++ b/index.html
@@ -419,9 +419,11 @@
   position:relative;border-radius:12px;overflow:hidden;
   border:1px solid var(--border-bright);background:#fff;
   box-shadow:var(--shadow);
-  width:100%;height:640px;display:block;background:#e8f4ea;
+  width:100%;height:860px;display:block;background:#e8f4ea;
 }
 .skewt-box canvas{position:absolute;inset:0;width:100%;height:100%;display:block;}
+@media(min-width:1100px){.skewt-box{height:940px;}}
+@media(min-width:1400px){.skewt-box{height:1020px;}}
 @media(max-width:600px){.skewt-box{height:520px;}}
 @media(max-width:400px){.skewt-box{height:460px;}}
 /* Labels/names hidden — show only the map image. */
@@ -4573,14 +4575,14 @@
   // axis (not overlaid on the plot itself), so the numbers are baked into
   // the same canvas as the chart — printing or saving the image gets both
   // together. Skipped below a width where it'd crowd the plot too much.
-  const indicesW=W>=480?92:(W>=360?76:68);
+  const indicesW=W>=1100?130:(W>=800?108:(W>=480?92:(W>=360?76:68)));
   // Summary block: a fixed number of full-width wrapped lines reserved
   // below the title, for the same plain-English analysis text shown above
   // the chart in HTML — reads better wrapped across the full width than
   // squeezed into the narrow indices column. Narrower canvases wrap the
   // same text into more, shorter lines, so reserve more of them.
   const summaryLines=W>=700?2:(W>=450?3:4);
-  const pad={l:42,r:52+indicesW,t:12+titleLines*13+4,b:28};
+  const pad={l:W>=900?52:42,r:(W>=900?64:52)+indicesW,t:12+titleLines*13+4,b:W>=900?34:28};
   const plotW=Math.max(10,W-pad.l-pad.r), plotH=Math.max(10,H-pad.t-pad.b);
   const PBOT=1000,PTOP=100,TMIN=-40,TMAX=60;
   const SKEW=plotW/plotH;
